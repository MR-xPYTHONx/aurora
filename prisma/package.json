{
  "name": "@aurora/prisma",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --name init",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.4.1"
  },
  "devDependencies": {
    "prisma": "^5.4.1"
  }
}
